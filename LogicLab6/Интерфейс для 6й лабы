Описание интерфейса для лабы 6 (прототип ЭС):
1) Вариант 1 - только алгоритм Мамдани:
- задаются 3 файла: с функциями, с переменными и с правилами.
- задаются значения лингвистических переменных h (глубина) и d (длина).
- на выходе значение стоимости m.
Форматы файлов:
- функции: id, a, b, c, d, comment;
- переменные: name, sname и массив funcs;
- правила: id, precondition (массив из структур, содержащих 2 поля: var и func), consequence (структура из 2х полей: var и func)

На графическом интерфейсе:
- поля задания файлов,
- поля задания лингвистических величин,
- отображение графиков для каждой лингвистической переменной (3 графика по 3 трапеции на каждом)
- отображение альфа-уровней для каждого правила: сначала 2 предпосылки, потом 1 следствие. Т.к. 9 правил, то получается 27 графиков.  Для нашего примера это такие наборы графиков:
(10,20,30)
(10,21,30)
(10,22,31)
(11,20,31)
(11,21,31)
(11,22,32)
(12,20,31)
(12,21,32)
(12,22,32)
Желательно, конечно, бы показать, что альфа-уровень на следствии соответствует минимальному альфа-уровню из предпосылок (например, пунктирной линией).
- совмещение этих трёх графиков в один с отображением центроида - найденного значения m.

Данные для графика (массивы точек (x,y), которые достаточно между собой отрезками соединить, значения альфа-уровней на графиках и значение m) можно брать из сервиса - я подготовлю API и скину за вск.

2) Вариант 2 - сделать интерфейс и для алгоритма Суджено.
- задаются 3 файла: с функциями, с переменными и с правилами.
- задаются значения лингвистических переменных h (глубина) и d (длина).
- на выходе значение стоимости m.
Форматы файлов:
- функции: id, ka, kb, comment;
- переменные: name, sname и массив funcs;
- правила: id, precondition (массив из структур, содержащих 2 поля: var и func), consequence (структура из 2х полей: var и func)

На графическом интерфейсе:
- поля задания файлов,
- поля задания лингвистических величин,
- отображение графиков для каждой исходной лингвистической переменной (2 графика по 3 терма на каждом)
- отображение альфа-уровней для каждого правила: т.е. 2 графика для предпосылок, указание/выделение значения минимального альфа-уровня и конкретное значение выходной переменной. Т.к. 9 правил, то получается 18 графиков. Для нашего примера это такие наборы графиков:
(10,20)
(10,21)
(10,22)
(11,20)
(11,21)
(11,22)
(12,20)
(12,21)
(12,22)
- вывод найденного значения m.

Если реализуем второй вариант тоже - накидаю API и скину.

